datasource db {
    provider = "mysql"
    url      = env("DATABASE_URL")
}

model Domains {
    id        Int      @id @default(autoincrement())
    createdAt DateTime @default(now())
    url       String   @unique
}
